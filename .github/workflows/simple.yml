name: Simple shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: eccho "La classe les gars"
      - name: echo another string
        run: | 
          node -v
          npm -v